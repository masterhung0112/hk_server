package gcp_functions

import (
  "net/http"
)

func user_gcp_func(w http.ResponseWriter, r *http.Request) {

  // Initialize the server function
  // th := SetupHelper()//.InitBasic()
  // defer th.TearDown()

  // Call function to handler
}